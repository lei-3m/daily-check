# Daily Check — 작업 규칙

하루 단위 체크리스트 웹앱. React + TypeScript + Vite + Tailwind.
Supabase(인증·DB), Vercel(배포). **백엔드 서버는 없습니다.**

전체 사양은 `docs/기획서-daily-check.md`를 먼저 읽으세요.

## 절대 규칙

1. **`toISOString()` 사용 금지.** 날짜 키는 로컬 타임존 기준 "YYYY-MM-DD".
   UTC 변환으로 한국 시간 오전 9시 이전에 하루가 밀립니다.
   `src/lib/date.ts`의 함수를 쓰세요.

2. **📋 복사의 출력 형식은 마크다운 `- [ ]` / `- [x]`.**
   화면 체크박스 모양이 바뀌어도 클립보드 내용은 절대 바뀌면 안 됩니다.
   이것이 이 앱의 존재 이유입니다.

3. **저장소 접근은 `src/lib/storage.ts`만 통과.**
   컴포넌트에서 localStorage나 supabase를 직접 호출하지 마세요.

4. **`service_role` 키를 코드에 넣지 마세요.**
   클라이언트는 Publishable(anon) key만 사용합니다.

5. **백엔드 서버, API 라우트, Node 런타임을 만들지 마세요.**
   빌드 결과물은 항상 정적 파일이어야 합니다.

## UI 규칙

6. **hover에서만 나타나는 UI 금지.** 주 사용 기기가 모바일입니다.
   기본 상태에서 충분히 보이게 하고, hover 강조는
   `@media (hover: hover)` 안에서만 추가하세요.
   터치 영역은 최소 44x44.

7. **모바일 입력 처리를 확인하세요.**
   - 키보드 Enter가 다음 필드로 넘어가지 않도록 `enterKeyHint`와 `preventDefault`
   - 버튼 클릭 시 입력창 blur로 값이 사라지지 않도록 처리

8. **화면에 보이는 것을 클릭하면 그 자리에서 반응합니다.**
   화면 전환은 명시적인 이동 버튼일 때만.
   `›`는 이동, `▾`는 그 자리에서 펼침을 뜻합니다.

9. **할 일 목록이 주인공입니다.**
   위쪽 블록을 추가하거나 키울 때는 할 일이 첫 화면 아래로 밀리지 않는지 확인하세요.

10. **달성률(%)을 표시하지 마세요.** `2/5` 같은 개수만 씁니다.
    퍼센트는 바쁜 날을 낮게 표시해 성적표처럼 읽힙니다.

11. **토글 버튼의 글자와 아이콘은 현재 상태를 반영합니다.**
    지금 누르면 무슨 일이 일어나는지를 표시하세요.
    (편집 중 → "완료", 펼쳐진 상태 → "접기")

12. **달성률(%)을 표시하지 마세요.** `2/5` 같은 개수만 씁니다.

13. **클릭 영역은 눈에 보이는 만큼입니다.**
    "열어서 추가" 같은 안내 글자도 눌러야 하는 곳이라면 함께 동작해야 합니다.

14. 화면 전환은 브라우저 히스토리에 반영되어야 합니다.
    폰 뒤로가기가 앱을 나가버리면 안 됩니다.

15. Tailwind 색상 클래스(bg-slate-50 등)를 직접 쓰지 마세요.
    CSS 변수를 사용하세요. dark: 변형을 매번 붙이면 반드시 빠뜨립니다.

16. 레이아웃 변경 시 380px(폰)과 1200px(PC) 양쪽을 확인하세요.
    한쪽만 보고 고치면 다른 쪽이 깨집니다.

## 작업 방식

- 한 번에 하나씩. 지시하지 않은 기능을 추가하지 마세요.
- 기획서 "비목표"와 "보류" 항목에 해당하는 것을 만들지 마세요.
- 변경 후 폰 크기(약 380px)에서 레이아웃이 깨지지 않는지 확인하세요.
- **브라우저 자동 검증(합성 제스처, 헤드리스 조작)은 하지 마세요.**
  실제 확인은 사용자가 폰에서 합니다. 자동 검증은 통과해도
  실기기에서 실패하는 경우가 많고 시간이 오래 걸립니다.

## 디버깅

- 브라우저의 CORS 오류는 **404나 401의 위장**일 때가 많습니다.
  오류 응답에는 CORS 헤더가 없어 브라우저가 CORS 문제로 표시합니다.
  Edge Function 문제는 curl로 직접 확인하세요.

      curl.exe -i -X OPTIONS https://<project>.supabase.co/functions/v1/<name>

- 코드를 고쳐도 반영되지 않으면 서비스 워커 캐시를 먼저 의심하세요.